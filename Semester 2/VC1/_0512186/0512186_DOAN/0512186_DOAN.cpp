// 0512186_DOAN.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include<stdio.h>
#include<conio.h>

int main(int argc, char* argv[])
{
	Phan2();
/*
	char s[1000];
	BIGINT K,N;
	BIGINT *kq;
	int k;

	fflush(stdin);
	printf("Nhap so thu nhat:");

	gets(s);
	input(K,s);

	fflush(stdin);
	printf("Nhap so thu hai :");
	gets(s);
	input(N,s);

	printf("Cho biet toan tu(1-cong,2-tru,3-nhan,4-sosanh):");
	scanf("%d",&k);
	printf("Ket qua:");
	int ss=sosanh(K,N);
	int ss1=sosanh1(K,N);
	switch(k)
	{
	case 1:
		
		kq=CongBIGINT(K,N);
		xuat(*kq);
		break;
	
	case 2:
		kq=TruBIGINT(K,N);
		xuat(*kq);
		break;
	case 3:
		kq=NhanBIGINT (K,N);
		xuat(*kq);
		break;
	case 4:
		int ss=sosanh(K,N);
		if(ss==1)
		{
			xuat(K);
			printf(">");
			xuat(N);
		}
		if(ss==-1)
		{
			xuat(K);
			printf("<");
			xuat(N);
		}
		if(ss==0)
		{
			xuat(K);
			printf("=");
			xuat(N);
		}
		printf("\n");
		break;
	}
*/	
	return 0;
}
